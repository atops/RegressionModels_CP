mtcars
summary(mtcars)
lm(mpg, cyl + hp, mtcars)
lm(mpg ~ cyl + hp, mtcars)
lm(mpg ~ cyl + disp + hp + drat + wt + qsec + vs + am + gear + carb, mtcars)
summary(fit)
fit <- lm(mpg ~ cyl + disp + hp + drat + wt + qsec + vs + am + gear + carb, mtcars)
summary(fit)
??mtcars
plot(mtcars)
plot(mtcars)
library(car)
#plot(mtcars)
scatterplot.matrix(~mpg+disp+hp+drat+wt+qsec+vs+am+gear+carb|cyl, data=mtcars, main="Three Cylinder Options")
install.packages("car")
install.packages("car")
#library(car)
#plot(mtcars)
pairs(mtcars)
#scatterplot.matrix(~mpg+disp+hp+drat+wt+qsec+vs+am+gear+carb|cyl, data=mtcars, main="Three Cylinder Options")
fit <- lm(mpg ~ wt + qsec + am, mtcars)
summary(fit)
q()
pairs(mtcars)
data(mtcars)
#summary(mtcars)
pairs(mtcars)
fit <- lm(mpg ~ cyl + wt, mtcars)
fit$coefficients
lm(mpg ~ cyl, mtcars)
lm(mpg ~ cyl + wt, mtcars)
summary(fit)$coef
fit <- lm(mpg ~ cyl + wt, mtcars)
summary(fit)$coefficients
basemodel <- lm(mpg ~ cyl + wt, mtcars)
summary(basemodel)$coefficients
model2 <- lm(mpg ~ cyl + wt + am, mtcars)
summary(model2)$coefficients
anova(basemodel, model2)
mtcars
basemodel <- lm(mpg ~ factor(cyl) + wt, mtcars)
summary(basemodel)$coefficients
model2 <- lm(mpg ~ factor(cyl) + wt + factor(am), mtcars)
summary(model2)$coefficients
anova(basemodel, model2)
pairs(mtcars)
basemodel
dfbeta(basemodel)
hatvalues(basemodel)
basemodel
model2
dfbetas(basemodel)
?dfbetas
plot(hatvalues(basemodel))
plot(hatvalues(model2))
model3 <- lm(mpg ~ factor(cyl) + wt + qsec, mtcars)
summary(model3)
summary(basemodel)
model4 <- lm(mpg ~ factor(cyl) + wt + hp, mtcars)
summary(model4)
model4 <- lm(mpg ~ factor(cyl) + wt + disp, mtcars)
summary(model4)
anova(basemodel, model4)
?with
with(mtcars, plot(mpg, hp))
with(mtcars, plot(mpg, log(hp)))
with(mtcars, abline(lm(mpg ~ log(hp))))
library(ggplot2)
g <- qplot(x=log(hp), y=mpg, data=mtcars)
g
g + geom_line(lm(mpg ~ log(hp)))
g + geom_line(lm(mpg ~ log(hp)), data=mtcars)
abline(lm(mpg ~ log(hp)))
abline(lm(mtcars$mpg ~ log(mtcars$hp)))
plot(abline(lm(mtcars$mpg ~ log(mtcars$hp))))
lm(mtcars$mpg ~ log(mtcars$hp))
summary(lm(mtcars$mpg ~ log(mtcars$hp)))
model5 <- lm(mpg ~ factor(cyl) + wt + log(hp), mtcars)
summary(model5)
anova(basemodel, model5)
summary(lm(mpg ~ log(hp) + wt, mtcars))
summary(basemodel)
summary(lm(mpg ~ hp + wt, mtcars))
summary(lm(mpg ~ log(hp) + wt, mtcars))
summary(lm(mpg ~ log(disp) + wt, mtcars))
summary(model2)
summary(basemodel)
